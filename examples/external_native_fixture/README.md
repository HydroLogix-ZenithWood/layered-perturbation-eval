# External native real-array interface fixture

This is rep1, first frozen draw, observed arm A, selected by metadata before outcomes. It is not an extra biological replicate. Ten fixed targets, 768 fixed-model penultimate CLS coordinates, real observed response and real executed target/nearest-nonpanel deletion vectors. Native input cells, PCAfit and observed reference cells are strictly disjoint. Both prediction configs use identical observed arrays and gallery.

Run unchanged portable `layered-eval score-predictions target_config.json --output /new/directory` and then neighbour_config.json. Compare score_matrix.npz with the corresponding expected matrix. Do not infer novel statistical power from this execution fixture.
